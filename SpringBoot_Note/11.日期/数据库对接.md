## 概述

> - **数据库中的日期字段筛选**：你需要利用 SQL 的日期函数或者条件查询来筛选出三个月内的数据。
> - **后端处理查询结果并输出**：后端接收到查询结果后，可以将数据格式化后输出给前端。
> - **查询的时间范围计算**：可以使用   **==Java 的日期 API==**  动态计算当前日期的前三个月，作为查询条件。



## 场景

> - 假设你有一个 `reservation` 表，存储用户的预约记录，其中包含 `user_id` 和 `create_time` 字段。现在的需求是获取某个用户在过去三个月内的所有预约记录。

